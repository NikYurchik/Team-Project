Касатально реализации пунктов ТЗ:
1. [Антон] Дополнить функционал для адреса, eMail и ФИО, добавив новые функции в класс Record.
           ФИО, адрес и eMail - это отдельные классы на базе класса Field. В этих же классах проверки корректности (ФИО и eMail)
                                выполняются setter-ами.
           Setter класса ФИО (FullName) проверяет значение на наличие только букв и минуса.
           Setter класса Email проверяет значение по аналогии поиска в задании 15 модуля 5.
           В setter класса Phone добавить проверку корректности номера (только цифры, круглые скобки, минус и лидирующий +),
           длину номера не контролируем.
           eMail в AddressBook сохранять списком, аналогично телефонам.
           Адрес храним в единственном экземпляре. Можно и списком, но тогда для каждого адреса нужен дополнительный признак, что это за адрес.
           Добавить необходимые функции в классс AddressBook:
           - добавление/изменение адреса;
           - добавление/изменения eMail;
           - добавление/изменения ФИО.
           Каждая функция первым параметром принимает Name контакта, вторым либо строку со значением, либо объект соответствующего класса.
           Для функций измерения должен быть 3-й параметр, который тоже может быть либо строка со значением, либо объект соответствующего класса.
           Добавить в функции вывода записей контактов новые реквизиты (ФИО, eMail и адрес).

2. [Иван] Добавить функцию вывода списка контактов, день рождения которых будет от завтрашнего дня до указанного в параметре фнкции.
          Например, параметр =7 - выдать список контактов, у которых день рождения в ближайшие 7 дней.

3. [Антон] Добавить начальные проверки номера телефона и eMail (см. п.1).

4. [Антон] В моей реализации уже есть сквозной поиск, возможно его нужно как-то расширить.
           Сквозной поиск подстроки (фрагмета строки):
           - перебираются все записи контактов;
           - по каждой записи формируется строка содержащая все реквизиты контакта;
           - если в сформированной строке содержится искомый фрагмент, то строка сохраняется для вывода.

5. [Иван] См. п.2

6-10. [Павел] Это разработка нового функционала. Соответственно, добавить в список допустимых команд новые команды.
              Реализовать отдельным классом в отдельном модуле.

11. [Юрий] Опционально. Есть готовая реализация. Оформить отдельным модулем проекта и добавить соответствующую команду в список команд.

*. [Максим] Продумать реализацию. Если есть какой-то готовый вариант рассмотреть возможность встраивания его в парсер команд.
            Решение найдено. Нужно разработать отдельную функцию, которая будет принимать 2 параметра:
            - введенная команда;
            - список имеющихся команд бота.
            Функция должна возвращать строку со списком подходящих команд через ",".
            Текст окончательного сообщения будет формироваться в парсере.

**. [Юрий] Расширить функционал парсера команд, чтобы была возможность как выполнить любую операцию одной командой с параметрами,
           так и с интерактивным запросом параметров команды.



[Павел] Заметки:
1. Сохранять дату создания/редактирования заметки.
2. Сортировка по дате создания/редактирования при выводе или при сохранении. ??? Продумать варианты ???
3. Редактирование:
   - выбор заметки по тегу;
   - если 1 заметка, то сразу ее редактируем;
   - если несколько, то выводим предварительный список: п.н. записи, дата создания/редактирования и ~50 символов заметки.
     пользователь вводит п.н. заметки и она выводится на редактирование.
4. Тегов на каждой заметке может бути несколько. Нужно добавить функции добавления и удаления тега у заметки.
5. Добавить поиск по тегу - это будет основной поиск. Функции поиска разделить - поиск по тегу, поиск по содержимому.
6. Удаление заметки по тегу.  Реализовать аналогично редактированию.



[Павел и Юрий]
Опционально - поискать вариант вывода редактируемой заметки на экран консоли с размещением её в буфере ввода (input).
Решенеия найдено, отправлен запрос ментору.

Это будет большое облегчение для клиента.
Можно будет реализовать подобное редактирование любого реквизита адресной книги.

Опционально - добавить команды настройки бота. Например выводить/не выводить список контактов, у которых ДР сегодня/завтра.
Пока я вижу 3 настройки:
- запрашивать ли в командном режиме недостающие реквизиты контакта;
- выводить ли при запуске бота список контактов, у которых день рождения попадает в заданный период от текущего дня;
- количство дней от текущего дня для вывода списка контактов, у которых день рождения попадает в этот период.
